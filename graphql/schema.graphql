type Query {
    getAllProducts(filter: ProductFilter): [Product]
}
type Product {
    id: String
    description: String
    price: Float
}
type Mutation {
    createProduct(input: InputProduct): Product
}
input InputProduct {
    id: String
    description: String!
    price: Float!
}
input ProductFilter {
    categoryId: String
}